include $(TOPDIR)/rules.mk

PKG_NAME:=luci-app-qwdtt
PKG_VERSION:=1.0.0
PKG_RELEASE:=1

LUCI_TITLE:=qWDTT - web interface
LUCI_DEPENDS:=+qwdtt
LUCI_PKGARCH:=all
LUCI_DESCRIPTION:=LuCI web interface for the qWDTT client: service status, \
	settings and diagnostics pages.

include ../../luci.mk

# call BuildPackage - OpenWrt buildroot signature