# Инструкция: qWDTT на OpenWrt

Два компонента:

- **qwdtt-openwrt** — RAW-IP клиент qWDTT для роутера (поднимает интерфейс `qwdtt0` и гонит через туннель трафик локальной сети);
- **luci-app-qwdtt** — веб-интерфейс (LuCI) для управления клиентом: обзор, настройки, диагностика.

Всё берётся из раздела [Releases](../../releases/latest).

---

## 1. Установка клиента

1. Определите архитектуру роутера:

   ```sh
   uname -m
   ```

2. Скачайте из [Releases](../../releases/latest) архив для своей архитектуры:

   | Артефакт | Для чего |
   | --- | --- |
   | `qwdtt-openwrt-x86_64.tar.gz` | x86-роутеры и виртуальные машины |
   | `qwdtt-openwrt-aarch64.tar.gz` | современные ARM64-роутеры |
   | `qwdtt-openwrt-armv7.tar.gz` | 32-битные ARMv7-устройства |
   | `qwdtt-openwrt-mipsel.tar.gz` | старые MIPS little-endian роутеры |

3. Установите на роутере от `root`:

   ```sh
   tar -xzf qwdtt-openwrt-aarch64.tar.gz
   cd qwdtt-openwrt-aarch64
   ./install.sh
   ```

4. Если установщик попросит зависимости, установите их и повторите установку:

   ```sh
   # OpenWrt с apk (23.05+/25.12)
   apk update && apk add ip-full kmod-tun ca-bundle

   # старые OpenWrt с opkg
   opkg update && opkg install ip-full kmod-tun ca-bundle
   ```

## 2. Настройка подключения

Файл конфигурации — `/etc/qwdtt/config.json`:

```json
{
  "peer": "YOUR_VPS_IP:56003",
  "hashes": ["YOUR_VK_CALL_HASH"],
  "password": "YOUR_CONNECTION_PASSWORD",
  "device_id": "openwrt-router",
  "workers": 9,
  "dns": "yandex",
  "obfs": "audio",
  "captcha_mode": "auto",
  "vk_auth": "anonymous",
  "vk_anon_path": "vkcalls",
  "no_dtls": false,
  "turn_tcp": false,
  "tun_name": "qwdtt0",
  "lan_interface": "br-lan"
}
```

Важные поля:

- **`peer`** — адрес сервера RAW-TUN в формате `host:порт`. Нужен именно RAW-листенер —
  обычно UDP-порт **56003**. Порт DTLS (**56000**) для этого клиента не подходит:
  подключение будет уходить в таймаут.
- **`password`** и **`hashes`** — данные выдачи VK. Не публикуйте и не пересылайте их.
- **`lan_interface`** — сетевой интерфейс LAN. По умолчанию `br-lan`; если в вашей сборке
  LAN называется иначе, исправьте.
- **`workers`** — число воркеров (9…108). Чем больше, тем выше нагрузка на CPU роутера.
- **`tun_name`** — если меняете, обновите и устройство зоны `qwdtt` во firewall.

Пароль и хеш также можно заполнить через веб-интерфейс — см. раздел 4.

Запустите клиент:

```sh
uci set qwdtt.main.enabled='1'
uci commit qwdtt
/etc/init.d/qwdtt start
```

Проверка:

```sh
logread -e qwdtt        # должно появиться «RAW-конфиг получен», затем «TUN подключён, трафик пошёл»
ip addr show qwdtt0
ip rule show
ip route show table 51820
```

Самопроверка TUN без данных VK:

```sh
/usr/bin/qwdtt-client -rawtun-self-test 10.70.0.2
```

Остановить клиент:

```sh
/etc/init.d/qwdtt stop
```

## 3. Установка веб-интерфейса (LuCI)

1. Скачайте из [Releases](../../releases/latest) артефакт вида
   `luci-app-qwdtt_<версия>-1_all.ipk` (например `luci-app-qwdtt_1.0.4-1_all.ipk`).
2. Установите после клиента:

   ```sh
   # opkg
   opkg install luci-app-qwdtt_1.0.4-1_all.ipk

   # apk
   apk add --allow-untrusted luci-app-qwdtt_1.0.4-1_all.ipk
   ```

3. Войдите в LuCI заново (чтобы применились права доступа). Раздел **qWDTT**
   появится в меню *Службы*.

## 4. Работа с веб-интерфейсом

Обновление состояния страниц — каждые 3 секунды.

### Обзор

- Статус службы и кнопки **Запустить / Остановить**.
- Адрес TUN-интерфейса, объём трафика.
- Живые скорости приёма/передачи.
- Сервер, число воркеров, количество VK-хешей.
- Кнопка **Открыть диагностику** — быстрый переход к диагностике при проблеме.

### Настройки

- Адрес сервера (`host:порт`).
- Пароль подключения с кнопкой показать/скрыть.
- VK-хеши (добавление/удаление).
- Воркеры: пресеты 9…108.
- LAN-интерфейс: вводится вручную или выбирается из списка найденных на роутере.
- Блок «Дополнительно»: `no_dtls`, `turn_tcp`, `tun_name`.
- Импорт/экспорт профиля подключения.
- Кнопки **Сохранить** и **Перезапустить службу**.

> Поля settings повторяют `/etc/qwdtt/config.json`. Изменения применяются
> сохранением и перезапуском службы.

### Диагностика

- Сводка: служба, TUN-адрес, трафик, правило маршрутизации (`ip rule`).
- Кнопка **Проверить VK-хеши**.
- Кнопка **Обновить** — перечитать данные.
- Кнопка **Скопировать диагностический отчёт** — готовый текст для отправки
  в поддержку (трафик показан, пароль и хеши в отчёт не попадают).

## 5. Обновление

При выходе нового релиза:

```sh
# клиент
tar -xzf qwdtt-openwrt-aarch64.tar.gz
cd qwdtt-openwrt-aarch64
./install.sh
/etc/init.d/qwdtt restart

# веб-интерфейс (opkg перезапишет старую версию)
opkg install --force-reinstall luci-app-qwdtt_1.0.4-1_all.ipk
# или для apk
apk add --allow-untrusted --force-overwrite luci-app-qwdtt_1.0.4-1_all.ipk
```

Настройки (`config.json`) при обновлении сохраняются.

## 6. Выпуск новой версии (для владельца репозитория)

1. Внесите изменения в `luci-app-qwdtt/` (`htdocs`, `root`, `po`) и/или в клиент.
2. Локальная проверка сборки ipk:

   ```sh
   node build-ipk.js
   ```

   Артефакт `luci-app-qwdtt_<версия>-1_all.ipk` в коммит не идёт — его собирает CI.
3. Закоммитьте и запушьте в `main`.
4. Откройте вкладку **Actions** → workflow **OpenWrt RAW client** → **Run workflow**,
   укажите версию (например `v1.0.5`) и запустите.

   Workflow соберёт клиент для всех архитектур, соберёт ipk нужной версии и
   опубликует GitHub Release с архивами и пакетом интерфейса.

> Пуш тегов вида `v1.0.x` тоже запускает сборку, но на форках триггер по push
> может не сработать — надёжнее запускать вручную кнопкой Run workflow.